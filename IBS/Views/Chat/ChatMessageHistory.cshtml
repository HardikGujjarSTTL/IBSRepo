@model IBS.Models.ChatMessage
<ul class="chatBodyText">
    @{
        if (Model.lstMsg.Count > 0)
        {
            foreach (var item in Model.lstMsg)
            {
                if (Model.Master_ID == item.msg_send_ID)
                {
                    <li class="sendTextHolder">
                        <div class="sendText">@item.message</div>
                    </li>
                }
                else
                {
                    <li class="reciveTextHolder">
                        <div class="recieveText">@item.message</div>
                    </li>
                }
            }
        }
    }
</ul>

<div class="chatInputArea">
    <input type="text" id="txtMsg" />
    <button class="submitChatBtn" id="btnSave" onclick="SaveMessage();"><span class="fa fa-paper-plane"></span></button>
</div>

<script>
    $(function () {
        //SaveMessage();
    })
</script>